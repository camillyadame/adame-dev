# Correção do asset da mascote — Hero

- Removida a borda/fundo branco que fazia a arte parecer um placeholder.
- Removida a legenda editorial original do arquivo pequeno; o Hero usa apenas a mascote.
- Asset do Hero reprocessado em 1200 × 1681 px para servir adequadamente às densidades de tela atuais.
- Hero redesenhado para reservar uma coluna real de 360–390 px à mascote em desktop.
- Mascote reposicionada como elemento principal da composição, com moldura e microelementos da identidade Adame.dev.
- Localização saiu da mesma linha do status e ganhou linha própria para evitar o texto espremido.
- CTA final também passou a usar uma versão limpa e ampliada da variação rosa (1200 × 1643 px).

## Observação de validação
A instalação de dependências excedeu o tempo disponível no ambiente de geração. O ZIP está sem `node_modules` e `.next`, como deve ser para entrega. Rode localmente `npm install` e `npm run dev` para a revisão visual.
